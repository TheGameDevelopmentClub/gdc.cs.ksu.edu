import { ScrollOnClickDirective } from './scroll-on-click.directive';

describe('ScrollOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollOnClickDirective(null);
    expect(directive).toBeTruthy();
  });
});
